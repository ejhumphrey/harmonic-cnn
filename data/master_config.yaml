paths:
  data_dir: "~/data"
  extract_dir: "~/data/ismir2016-wcqt-data"
  model_dir: "~/data/ismir2016-wcqt-models"

datasets: ["rwc", "uiowa", "philharmonia"]

dataframes:
  datasets: "datasets_df.json"
  notes: "notes_df.pkl"
  features: "features_df.pkl"

extract:
  max_duration: 2.0

features:
  cqt:
    num_cpus: -1
    skip_existing: false

model: "cqt_iX_c2f2_oY"

experiment:
  params_dir: "params"
  params_format: "params{0:0>4}.npz"
  config_path: "config.yaml"
  training_loss: "training_loss.npy"

training:
  t_len: 8
  max_iterations: 100000
  max_time: 8000
  batch_size: 60
  n_targets: 12
  iteration_print_frequency: 100
  iteration_write_frequency: 500
  hyperparameters:
    learning_rate: 0.01
    momentum: 0.9
  hold_out_set: "philharmonia"
  max_files_per_class: null
