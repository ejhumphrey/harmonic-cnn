paths:
  data_dir: "~/data"
  extract_dir: "~/data/ismir2016-wcqt-data"

datasets:
  rwc:
  uiowa:
  philharmonia:

dataframes:
  datasets: "datasets.json"
  notes: "notes.pkl"

extract:
  max_duration: 2.0
