paths:
  data_dir: "~/data"
  extract_dir: "~/data/ismir2016-wcqt-data"

datasets:
  rwc:
  uiowa:
  philharmonia:

dataframes:
  datasets: "datasets_df.json"
  notes: "notes_df.pkl"
  features: "features_df.pkl"

extract:
  max_duration: 2.0

features:
  cqt:
    num_cpus: -1
    skip_existing: true
